<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="V102-1" author="platform" labels="tenant,users">
        <comment>Tenant users — profile data, linked to users_global by global_user_id</comment>

        <createTable tableName="users"
                     remarks="Профили пользователей в рамках тенанта. Auth-данные — в public.users_global.">
            <column name="id" type="UUID" defaultValueComputed="uuid_generate_v4()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <!-- Ссылка на глобального пользователя (cross-schema, без FK в БД) -->
            <column name="global_user_id" type="UUID"
                    remarks="Ссылка на public.users_global.id (без FK constraint из-за разных схем)">
                <constraints nullable="false" unique="true" uniqueConstraintName="uq_users_global_user_id"/>
            </column>
            <column name="email" type="VARCHAR(255)"
                    remarks="Денормализация для удобства запросов (дублирует users_global.email)">
                <constraints nullable="false"/>
            </column>
            <column name="first_name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="last_name" type="VARCHAR(100)">
                <constraints nullable="false"/>
            </column>
            <column name="middle_name" type="VARCHAR(100)"/>
            <column name="phone" type="VARCHAR(30)"/>
            <column name="avatar_url" type="TEXT"/>
            <column name="position" type="VARCHAR(255)" remarks="Должность"/>
            <column name="is_active" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="last_login_at" type="TIMESTAMPTZ"/>
            <column name="settings" type="JSONB" defaultValue="{}"
                    remarks="Персональные настройки: тема, язык, уведомления"/>
            <column name="created_at" type="TIMESTAMPTZ" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMPTZ" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- FTS по имени и email -->
        <sql>
            CREATE INDEX idx_users_fts ON users
                USING gin(to_tsvector('russian_unaccent',
                    coalesce(last_name,'') || ' ' ||
                    coalesce(first_name,'') || ' ' ||
                    coalesce(middle_name,'') || ' ' ||
                    coalesce(email,'')));

            CREATE TRIGGER trg_users_updated_at
                BEFORE UPDATE ON users
                FOR EACH ROW EXECUTE FUNCTION set_updated_at();
        </sql>

        <rollback>
            <dropTable tableName="users"/>
        </rollback>
    </changeSet>

    <changeSet id="V102-2" author="platform" labels="tenant,users,rbac">
        <comment>User ↔ Role many-to-many</comment>

        <createTable tableName="user_roles">
            <column name="user_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_ur_user"
                             references="users(id)"
                             deleteCascade="true"/>
            </column>
            <column name="role_id" type="UUID">
                <constraints nullable="false"
                             foreignKeyName="fk_ur_role"
                             references="roles(id)"
                             deleteCascade="true"/>
            </column>
            <column name="assigned_at" type="TIMESTAMPTZ" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addPrimaryKey tableName="user_roles"
                       columnNames="user_id, role_id"
                       constraintName="pk_user_roles"/>

        <createIndex tableName="user_roles" indexName="idx_ur_role_id">
            <column name="role_id"/>
        </createIndex>

        <rollback>
            <dropTable tableName="user_roles"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
